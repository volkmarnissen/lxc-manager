{
  "execute_on": "host:node-red",
  "name": "Import Flow",
  "description": "Import a Node-RED flow from a JSON file or URL",
  "parameters": [
    {
      "id": "flow_source",
      "name": "Flow Source",
      "type": "string",
      "required": true,
      "description": "URL or path to the flow JSON file to import"
    },
    {
      "id": "flow_password",
      "name": "Flow Password",
      "type": "string",
      "required": false,
      "advanced": true,
      "description": "Password for encrypted credentials in the flow (optional)"
    },
    {
      "id": "flow_password_confirm",
      "name": "Confirm Password",
      "type": "string",
      "required": false,
      "advanced": true,
      "description": "Confirm the password for encrypted credentials (optional)"
    }
  ],
  "commands": [
    {
      "name": "Import Flow",
      "script": "scripts/import-flow.sh"
    }
  ]
}

