{
  "execute_on": "lxc",
  "name": "Configure MariaDB",
  "description": "Configure MariaDB bind address and other settings",
  "parameters": [
    {
      "id": "bind_address",
      "name": "Bind Address",
      "type": "string",
      "default": "0.0.0.0",
      "required": true,
      "description": "IP address to bind to (0.0.0.0 for all interfaces, required for remote access)"
    },
    {
      "id": "datadir",
      "name": "Data Directory",
      "type": "string",
      "default": "/var/lib/mysql",
      "required": true,
      "description": "Data directory path"
    }
  ],
  "commands": [
    {
      "script": "configure-mariadb.sh"
    }
  ]
}
