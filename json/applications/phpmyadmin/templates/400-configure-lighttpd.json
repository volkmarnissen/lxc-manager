{
  "execute_on": "lxc",
  "name": "Configure Lighttpd",
  "description": "Configure lighttpd to enable FastCGI support and set HTTP port",
  "parameters": [
    {
      "id": "http_port",
      "name": "HTTP Port",
      "type": "string",
      "required": true,
      "description": "Port number for lighttpd HTTP server"
    }
  ],
  "commands": [
    {
      "script": "configure-lighttpd.sh",
      "description": "Enable mod_fastcgi in lighttpd configuration and set server port"
    }
  ]
}
