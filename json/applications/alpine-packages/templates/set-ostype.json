{
  "execute_on": "lxc",
  "name": "Set OS Type",
  "description": "Sets the OS type for the Alpine build environment",
  "commands": [
    {
      "command": "echo '{\"id\": \"ostype\", \"value\": \"alpine\"}'",
      "outputs": ["ostype"]
    }
  ]
}
