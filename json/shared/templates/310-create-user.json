{
  "execute_on": "lxc",
  "name": "Create User on LXC Container",
  "description": "Create a user inside the LXC container with specified username, optional uid, and optional gid. If uid/gid are not provided, the system will assign them automatically. Calls create-user.sh with template variables.",
  "commands": [
    {
      "script": "create-user.sh",
      "description": "Create a user inside the LXC container with specified username, optional uid, and optional gid. Calls create-user.sh with template variables."
    }
  ],
  "parameters": [
    {
      "id": "username",
      "name": "Username",
      "type": "string",
      "description": "The username to create inside the LXC container.",
      "required": true
    },
    {
      "id": "uid",
      "name": "UID",
      "type": "string",
      "advanced": true,
      "description": "Optional: The UID for the user. If not provided, the system will assign one automatically."
    },
    {
      "id": "gid",
      "name": "GID",
      "type": "string",
      "advanced": true,
      "description": "Optional: The GID for the user. If not provided, the system will assign one automatically." 
    }
  ]
}
