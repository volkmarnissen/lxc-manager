{
  "execute_on": "ve",
  "name": "List VM IDs",
  "description": "List all LXC container VM IDs on in the VE Cluster",
  "commands": [
    {
      "name": "List VM IDs",
      "script": "list-vm-ids.sh",
      "outputs": ["vm_ids"]
    }
  ]
}
