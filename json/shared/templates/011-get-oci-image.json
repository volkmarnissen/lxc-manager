{
  "execute_on": "ve",
  "name": "Get OCI Image",
  "description": "Download and prepare OCI image for Proxmox 9.1",
  "parameters": [
    {
      "id": "oci_image",
      "name": "OCI Image",
      "type": "string",
      "required": true,
      "description": "OCI image reference (e.g., docker://alpine:latest, docker://debian:bookworm, oci://ghcr.io/owner/repo:latest, oci://registry.example.com/image:tag)"
    },
    {
      "id": "storage",
      "name": "Storage",
      "type": "string",
      "default": "local",
      "description": "Storage where the OCI image should be stored",
      "advanced": true
    }
  ],
  "outputs": ["template_path", "ostype"],
  "commands": [
    {
      "name": "Get OCI Image",
      "script": "get-oci-image.sh"
    }
  ]
}

