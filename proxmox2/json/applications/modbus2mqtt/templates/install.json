{
    "$schema": "file:/Users/volkmar/proxmox2/schemas/template.schema.json", 
    "execute_on": "lxc",
    "name": "Install Modbus2Mqtt",
    "description": "Installs modbus2mqtt VM on Proxmox",
    "parameters": [{
        "name": "packagerpubkey",
        "type": "string",
        "required": true,
        "description": "Public key for verifying the Modbus2Mqtt package"
        },
        {
        "name": "packageurl",
        "type": "string",
        "required": true,
        "description": "URL of the Modbus2Mqtt package to be installed"
        }
    ],
    "commands": [
            {
                "type": "template",
                "name": "Download and Install Modbus2Mqtt",
                "execute": "050-download-and-inst-apk.json"
            }
        ]
}