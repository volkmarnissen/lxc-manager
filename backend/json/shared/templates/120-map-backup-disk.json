{
  "$schema": "file:/Users/volkmar/proxmox2/schemas/template.schema.json",
  "execute_on": "proxmox",

  "name": "Map Serial Disk",
  "description": "Map serial device to VM",
  "parameters": [
    {
      "name": "disk_on_ve",
      "type": "string",
      "required": false,
      "default": "/dev/sdb2",
      "description": "Path to the Disk device on the VE host"
    },
    {
      "name": "vm_id",
      "type": "string",
      "required": true,
      "description": "ID of the VM to which the disk device will be assigned"
    }
    {
      "name": "mounted_path",
      "type": "string",
      "required": true,
      "default": "/mnt/backupdisk",
      "description": "Mount path inside the LXC container"
    },
    {
      "name": "disk_size",
      "type": "string",
      "required": true,
      "default": "100",
      "description": "Disk size for the container in GB"
    }
  ],
  "commands": [
    {
      "type": "script",
      "name": "Map Disk Device",
      "execute": "map-disk-device.sh"
    }
  ]
}
